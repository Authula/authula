<div>
    <h3>Hi {{.InvitationEmail}},</h3>
    <p>You have been invited to join <strong>{{.OrganizationName}}</strong> on <strong>{{.AppName}}</strong> as <strong>{{.Role}}</strong>.</p>
    <p><a href="{{.AcceptLink}}" style="display:inline-block;background:#111827;color:#ffffff;text-decoration:none;padding:10px 16px;border-radius:8px;">Accept invitation</a></p>
    <p>If the button does not work, copy this link:</p>
    <p><a href="{{.AcceptLink}}">{{.AcceptLink}}</a></p>
</div>
